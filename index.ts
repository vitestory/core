import {
  buildTemplateView,
  mustacheUtils,
  renderTemplate,
} from "./src/utils/template";

export const utils = {
  template: {
    buildTemplateView,
    mustacheUtils,
    renderTemplate,
  },
};
